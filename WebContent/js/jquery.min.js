/**
 * 
 */

$(document).on("click", ".open-Modal", function (e) {

    e.preventDefault();

    var _self = $(this);

    var produtoCod = _self.data('id');
    $("#bookId").val(myBookId);

    $(_self.attr('href')).modal('show');
});